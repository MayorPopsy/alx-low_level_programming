#include "main.h"
/**
 * _puts - A function that prints string followed by a new line
 *
 * Return: 0
 */
void _puts_recursion(char *s)
{
	char *s = " puts with recursion";
	{
		putchar('\n');
	}
	return (void);
}
